<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="#FFECD1">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="14dp">

        <!-- Judul -->
        <TextView
            android:id="@+id/txtJudul"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="#000000"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgKerajinan"/>

        <!-- Caption -->
        <TextView
            android:id="@+id/txtCaption"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:textColor="#444444"
            android:lineSpacingExtra="2dp"
            android:maxLines="4"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/txtJudul"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@id/imgKerajinan"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <!-- Gambar Kerajinan -->
        <ImageView
            android:id="@+id/imgKerajinan"
            android:layout_width="90dp"
            android:layout_height="0dp"
            android:scaleType="centerCrop"
            android:background="@drawable/rounded_image"
            app:layout_constraintTop_toTopOf="@id/txtJudul"
            app:layout_constraintBottom_toBottomOf="@id/txtCaption"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- Icon Bookmark -->
        <ImageView
            android:id="@+id/iconBookmark"
            android:layout_width="22dp"
            android:layout_height="22dp"
            android:src="@drawable/ic_bookmark"
            app:layout_constraintTop_toTopOf="@id/imgKerajinan"
            app:layout_constraintEnd_toStartOf="@id/imgKerajinan"
            android:layout_marginEnd="8dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>
